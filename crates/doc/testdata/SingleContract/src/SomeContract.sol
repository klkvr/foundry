/// @notice Some contract
/// @dev Hello world
contract SomeContract {
    /// @param bar Bar param
    function foo(uint256 bar) external pure returns (bool) {}

    /// @param bar Bar param at struct
    struct SomeStruct {
        uint256 bar;
    }
}